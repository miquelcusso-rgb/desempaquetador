# Desempaquetador

Resuelve anagramas en español. Compatible con Scrabble (FISE) y Apalabrados. Filtros por longitud, comodines, puntuación.

## Stack
HTML/CSS/JS plano. Deploy en GitHub Pages.

## Desarrollo local
```bash
python3 -m http.server 8000
# Abre http://localhost:8000
```

## Deploy
Push a `main`. GitHub Pages sirve desde `/`.

Dominio configurado en `CNAME` → anagramas.example.com (actualizar cuando se compre).

## Próximos pasos
Ver `CLAUDE.md` y `~/Documents/sites-system/PROJECTS.md`.
